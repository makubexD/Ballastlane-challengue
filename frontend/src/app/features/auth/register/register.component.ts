import { Component } from '@angular/core';

@Component({
  selector: 'app-register',
  standalone: true,
  template: `<div data-testid="register-page">Register</div>`
})
export class RegisterComponent {}
