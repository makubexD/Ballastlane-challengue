# vitest 4.x conflicts with @angular/build's optional peer dep (vitest ^3.x).
# This project uses @analogjs/vite-plugin-angular + vitest 4 directly;
# @angular/build's built-in test runner is not used here.
legacy-peer-deps=true
